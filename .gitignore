# Go
/bin/
/dist/
*.test
*.out

# Extension build output
/extension/dist/

# tsnet state / local runtime (never commit node keys)
/state/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Extension signing key (public key lives in manifest.chromium.json)
*.pem

# Working notes live outside the repo (../tailtab-private-notes); never commit them.
/coordination/

# Local agent config (Claude Code, OpenCode). Skills reference the private notes and real tailnet names.
/.claude/
/.opencode/
